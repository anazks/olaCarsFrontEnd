const fs = require("fs"); let code = fs.readFileSync("src/pages/dashboards/shared/DriverDetail.tsx", "utf8"); code = code.replace("{/* Assigned Vehicle Section */}", "<div className=\"grid grid-cols-1 lg:grid-cols-2 gap-4 items-start\">\n            {/* Assigned Vehicle Section */}"); code = code.replace("{/* Vehicle Rent Contract Details - Always visible when vehicle assigned */}", "</div>\n\n            {/* Vehicle Rent Contract Details - Always visible when vehicle assigned */}"); fs.writeFileSync("src/pages/dashboards/shared/DriverDetail.tsx", code);
